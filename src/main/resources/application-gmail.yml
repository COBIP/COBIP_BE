spring:
  config:
    activate:
      on-profile: gmail
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${GMAIL_SMTP_USERNAME:${MAIL_USERNAME:}}
    password: ${GMAIL_SMTP_PASSWORD:${MAIL_PASSWORD:}}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

app:
  mail:
    from: ${MAIL_FROM:${GMAIL_SMTP_USERNAME:${MAIL_USERNAME:}}}
